YC Todo：之後如何繼續 Improve App ＆ 正確開發 / 發佈流程（補充筆記）
====================================================================

這一份是「完成第一版 macOS menubar app（Popover 版）」之後的實戰流程筆記。
目的：讓之後開發不混亂，知道什麼時候 dev、什麼時候 build、什麼時候要重新安裝。

--------------------------------------------------------------------
0) 你目前已完成的成果（Checkpoint）
--------------------------------------------------------------------

✅ React + Vite Todo UI（含 timer / 狀態 / localStorage）
✅ 使用 Tauri 打包成 macOS App（.app / .dmg）
✅ Tray / Menubar icon（點擊開關）
✅ Dock 隱藏（Accessory app）
✅ **已改成 macOS 原生 NSPopover 行為**
   - 全螢幕 Chrome / VS Code 都能浮在最上面
   - 行為類似 Proton VPN / Raycast
   - 使用 `tauri-plugin-nspopover`（macOS only）
✅ UI 已調整為 Popover 尺寸（填滿、不露底色）

--------------------------------------------------------------------
1) 之後只會在兩種模式之間切換
--------------------------------------------------------------------

------------------------------------------------------------
【模式 1】開發模式（改功能 / 改 UI / Debug）
------------------------------------------------------------
用途：
- 改 React / CSS / UI 排版
- 加新功能（timer、鎖定、排序）
- 改 tray / popover 行為
- Debug 問題

指令（專案根目錄）：
  npx tauri dev

特性：
- 需要 VS Code + terminal
- React 有熱更新（快）
- Rust / Tauri 會重新編譯（較慢）
- 只是測試，不是正式 App

👉 任何改動，一定先用這個模式測

------------------------------------------------------------
【模式 2】正式 App（實際使用 / 發佈）
------------------------------------------------------------
用途：
- 產生真正可用的 macOS app
- 不需要 VS Code / terminal
- 可拖進 Applications 使用

指令：
  npx tauri build

產物位置：
- .app：
  src-tauri/target/release/bundle/macos/YC Todo.app
- .dmg：
  src-tauri/target/release/bundle/dmg/YC_Todo_0.1.0_*.dmg

👉 每次想「用最新版本」都一定要重新 build

--------------------------------------------------------------------
2) 最重要的標準工作流程（一定要記住）
--------------------------------------------------------------------

你之後的開發循環永遠是：

  改功能 / 改 UI
        ↓
  npx tauri dev   （測試）
        ↓
      OK
        ↓
  npx tauri build （產生正式 app）
        ↓
  用 .dmg 安裝 或 覆蓋 Applications 裡的舊 app

⚠️ Menubar app 常見陷阱：
- 看起來關了，其實還在背景
- 更新後請「完全退出舊版 app」再重開
- 如果 Finder 沒更新，可執行：
  killall Finder

--------------------------------------------------------------------
3) NSPopover（macOS 原生 Popover）專用說明【重要】
--------------------------------------------------------------------

你現在使用的是 **Popover 方案**，有幾個「和一般 Tauri app 不同的地方」：

### (1) 依賴位置（一定要對）
- Rust 端：`src-tauri/Cargo.toml`
  - 使用 `tauri-plugin-nspopover`
- JS 端：`package.json`
  - 用 git URL（不是 npm registry）
- 權限：`src-tauri/capabilities/default.json`
  - 必須加 nspopover permissions
- Tray：
  - **不要在 tauri.conf.json 用 systemTray / trayIcon**
  - Tray icon 由 Rust 建立（避免 schema not allowed）

### (2) npm install 要不要跑？
- 只改 React / CSS → ❌ 不用
- 有改 `package.json`（新增依賴）→ ✅ 一定要跑
  npm install

⚠️ 注意：
`tauri-plugin-nspopover` 不是 npm 套件  
所以：
❌ npm i tauri-plugin-nspopover → 404  
✅ 在 package.json 用 git URL + npm install

--------------------------------------------------------------------
4) 常見情境 → 要跑哪個？
--------------------------------------------------------------------

Q1：我只是在調 UI、CSS、React state  
→ npx tauri dev

Q2：我在改 tray / popover / timer 行為  
→ npx tauri dev

Q3：我要每天用 / 給朋友用  
→ npx tauri build

Q4：build 了但 app 看起來沒變  
→ 你還在跑舊版（背景）
→ 完全關閉後重開，或重新安裝 dmg

Q5：全螢幕 Chrome 點 icon 不浮在最上面  
→ 還在用普通 window 模式  
→ 必須使用 NSPopover（你現在已完成）

--------------------------------------------------------------------
5) UI / CSS（Popover 專用原則）
--------------------------------------------------------------------

Popover UI 有幾個關鍵原則（你現在已實作）：

- body 背景設為 transparent
- menu-card：
  - width / height = 100%
  - 使用 padding 控制內距
- 用 flex + flex:1 讓中間 list 撐滿
- 不要用 body 居中（避免左右露底色）

👉 Popover 是「容器已固定大小」，不是一般網頁

--------------------------------------------------------------------
6) 依賴與設定的注意事項（避免踩雷）
--------------------------------------------------------------------

- Cargo.toml：
  - 只能有 **一個 `[dependencies]`**
  - 重複會導致奇怪編譯錯誤
- tauri.conf.json：
  - 出現 `not allowed` 通常是 schema 不支援
  - Popover + Tray 請交給 Rust / plugin
- plugin panic（unwrap None）：
  - 幾乎都是 tray / window id 沒對上

--------------------------------------------------------------------
7) 之後可以怎麼繼續升級 YC Todo？
--------------------------------------------------------------------

【UX / 產品等級】
- tray icon 顯示剩餘時間（12:34）
- timer 結束通知（Notification Center）
- 專注模式：進行中 task 鎖定其他操作
- 空狀態 / completed 動畫

【系統整合】
- 開機自動啟動（toggle）
- Global shortcut（⌘⇧T）
- 深色 / 淺色模式自動切換
- 未來可移植 Windows（非 NSPopover）

【發佈】
- .dmg 分享
- code signing / notarization（之後再做）
- 正式版本號管理

--------------------------------------------------------------------
8) 一句總結（定位）
--------------------------------------------------------------------

YC Todo 已經不是學習 Demo，而是：

「一個真正可以每天使用的 macOS menubar productivity app」

記住兩句話就夠：
- 改功能 → dev
- 要用 / 發佈 → build

--------------------------------------------------------------------
（完）
