============================================================
           Solidity Gas 優化全攻略：今日技術總結
============================================================

1. 奇偶判斷 (CheckEven)
------------------------------------------------------------
■ 省錢秘訣：用「位元運算」取代「模除運算」。
■ 實現方式：使用 (n & 1) == 0。
■ 為什麼：EVM 指令中，AND 比 MOD 便宜得多。

2. 字串比對 (CompareString)
------------------------------------------------------------
■ 省錢秘訣：先比對「長度」，再算「哈希」。
■ 實現方式：
  if (bytes(s1).length != bytes(s2).length) return false;
  return keccak256(abi.encodePacked(s1)) == keccak256(abi.encodePacked(s2));
■ 為什麼：哈希運算極貴，如果長度不同就不用算了，直接回傳。

3. 階乘運算 (Factorial)
------------------------------------------------------------
■ 省錢秘訣：關閉溢位檢查，使用遞減迴圈。
■ 實現方式：使用 `unchecked { ... }` 包裹運算，並用 n--。
■ 為什麼：Solidity 0.8+ 預設的溢位檢查在迴圈中會累積大量 Gas 成本。

4. 尋找最大值 (FindGreatest) - 你的 270 分考題
------------------------------------------------------------
■ 省錢秘訣：Calldata + Cache Length + Unchecked Counter。
■ 實現方式：
  - 參數設為 `calldata` (減少拷貝)。
  - `uint256 len = numbers.length;` (快取長度)。
  - `unchecked { ++i; }` (最精簡的計數器)。
■ 進階：使用 Inline Assembly (Yul) 繞過「陣列邊界檢查」。

5. 權限設定 (SetOwner)
------------------------------------------------------------
■ 省錢秘訣：Immutable + Custom Errors。
■ 實現方式：
  - 使用 `address public immutable OWNER;`。
  - 使用 `if (msg.sender != OWNER) revert NotOwner();`。
■ 為什麼：Immutable 不佔用 Storage Slot；Custom Errors 比 Require String 省錢。

6. 數值儲存 (Storage_saveVal)
------------------------------------------------------------
■ 省錢秘訣：External 可見性與 Slot 最小化。
■ 實現方式：
  - 函數標記為 `external`。
  - 了解 SSTORE 的 Gas 陷阱（從 0 改為 非0 最貴）。
■ 為什麼：External 減少記憶體拷貝，直接操作 Storage 插槽。

============================================================
           💡 核心心法：每一滴 Gas 都是錢 💡
============================================================
1. 讀寫 Storage 是最貴的 (SSTORE/SLOAD)。
2. 能在 Stack 算的就在 Stack 算，不要回頭讀變數。
3. 迴圈內部的優化會被放大 (放大倍數 = 迴圈次數)。
4. 善用 Assembly (Yul) 是衝向滿分 280 的唯一途徑。
============================================================